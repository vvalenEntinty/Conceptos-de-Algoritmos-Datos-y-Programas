program alcance4a;
var a,b: integer;
procedure uno;
begin
a := 1;
writeln(a);
end;
begin
a:= 1;
b:= 2;
uno;
writeln(b, a);
end.

Imprime 1 2 1

program alcance4b;
procedure uno;
begin
a := 1;
writeln(a);
end;
var a,b: integer;
begin
a:= 1;
b:= 2;
uno;
writeln(b, a);
end.

las variables a y b se declaran como variables globales despues del procedimiento UNO; por lo tanto a no esta declarada como global y esto hace que no compile
